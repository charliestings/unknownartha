import React from "react";
import ReactDOM from "react-dom/client";
import JnanaBrand from "./JnanaBrand.jsx"; // your component

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JnanaBrand />
  </React.StrictMode>
);